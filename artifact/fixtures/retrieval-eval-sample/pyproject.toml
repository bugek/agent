[tool.poetry]
name = "retrieval-eval-sample"
version = "0.0.1"
description = "Sample repository for planner retrieval evaluation"
authors = ["AI Code Agent <agent@example.com>"]

[tool.poetry.dependencies]
python = "^3.11"
