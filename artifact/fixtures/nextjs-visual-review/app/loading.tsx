export default function Loading() {
  return (
    <main
      style={{
        minHeight: "100vh",
        display: "grid",
        placeItems: "center",
        background: "#f4ecdf",
        color: "#5c483a",
      }}
    >
      Loading the editorial dashboard preview.
    </main>
  );
}