# Monitor Frontend

ReactFlow-based frontend for AI Code Agent monitor telemetry.

## Development

Install dependencies:

```bash
npm install
```

Run the frontend:

```bash
npm run dev
```

By default the app calls `http://127.0.0.1:8000/api/monitor`.
Override with `VITE_MONITOR_API_BASE` if the backend runs elsewhere.
